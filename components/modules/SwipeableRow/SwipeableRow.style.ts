export const STYLES = {
  container: "relative w-full overflow-hidden",
  actionLayer: "absolute inset-0 flex items-center justify-between",
  leftAction: "flex h-full items-center justify-start",
  rightAction: "flex h-full items-center justify-end",
} as const;
